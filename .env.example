# Environment Variables Template for STARK Application
# Copy this file to .env in server/ directory and fill in your values

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=5000
NODE_ENV=production

# ===========================================
# MONGODB DATABASE (REQUIRED)
# ===========================================
# Get your MongoDB Atlas connection string from:
# https://cloud.mongodb.com/
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/pakuniconnect?retryWrites=true&w=majority

# ===========================================
# JWT SECRET (REQUIRED)
# ===========================================
# Generate a strong secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production

# ===========================================
# CLIENT URL (REQUIRED)
# ===========================================
# For local development: http://localhost:5173
# For production (Vercel): https://your-app-name.vercel.app
CLIENT_URL=http://localhost:5173

# ===========================================
# EMAIL CONFIGURATION (OPTIONAL - for password reset)
# ===========================================
# For Gmail, use: smtp.gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email "From" address
SMTP_FROM=noreply@starkuniconnect.com
